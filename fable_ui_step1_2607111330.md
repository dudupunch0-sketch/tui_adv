# 이구학지 Web Storybook UI/UX 개선 플랜 — 수묵 천기록 (Plan by Fable, implementation by codex)

Date: 2026-07-11 (Rev 2, same day)
Baseline: `main` HEAD `2e32685` ("refactor(web): split storybook epilogue renderer [WP-C4] (#123)")

> **Rev 2 변경 요약**: 화면 구조를 "고정 HUD + 하단 dock 게임 프레임"(타사 게임
> template 유래)에서 **"서책 몰입형 + 정보 드로어"**로 변경했다. WP-V4~V7이
> 재작성되었고, 참조 게임 이미지 제거·문서 개정(구 WP-V10)은 선행 커밋에서 이미
> 완료되었다. canonical 시각 계약은 `docs/design/Mobile_Ink_Storybook_UI.md`.
Target: `web/` 렌더러 전용. 목표 플랫폼은 **모바일 세로형 웹 + Rust/WASM GameCore**.

이 문서는 구현 단계의 완전한 지시서다. 처음부터 끝까지 읽은 뒤 워크패키지를
**순서대로, 하나씩** 실행한다. 각 워크패키지가 끝나면: 검증 명령 실행 → git
커밋 1개 → 다음 워크패키지로 이동.

라인 번호는 baseline `2e32685` 기준이다. 편집을 시작하면 라인이 밀리므로
**함수/클래스/셀렉터 이름으로** 코드를 찾는다.

---

## 0. 이 작업이 무엇이고 무엇이 아닌가

이번 작업은 두 가지다.

1. **Phase U — UX 결함 수정**: 미감과 무관하게 지금 명백히 깨져 있는 플레이 UX를 고친다.
2. **Phase V — 수묵(水墨) 비주얼 리디자인**: "모바일 픽셀 게임북" 흉내(CSS gradient로
   그린 가짜 픽셀아트)를 버리고, `이구학지 — 천기록`의 정체성에 맞는
   **수묵·고서(古書) 미감**으로 전환한다. 모든 장면 일러스트는 코드(SVG)로 저작한다.

이전 리팩터링과 달리 **DOM/CSS의 관찰 가능한 모습은 크게 바뀐다.** 대신 아래
"계약 표면"은 절대 보존한다.

### 0.1 절대 불변 — Hard invariants

1. **`web/` 밖은 손대지 않는다.** `crates/`, `scripts/`, `src/tui_adv/`,
   fixtures, `web/src/data/generated/**` 전부 불변. 예외는 WP-0(파이썬 테스트
   경로 수정)과 WP-V10(설계 문서 개정)뿐이며 해당 WP에 명시된 파일만 수정한다.
2. **Renderer boundary**: Rust GameCore가 eligibility/outcome/ending/achievement의
   truth다. 렌더러는 `ScenePage`를 해석·표시만 하고 어떤 게임 규칙도 재계산하지
   않는다. `ScenePage` JSON 필드( [web/src/core/types.ts](web/src/core/types.ts) )는 frozen wire contract — 필드
   추가/개명 금지.
3. **액션 배선 계약 유지**: `<button class="choice-row" data-action-id data-action-kind>`,
   숫자키 실행(`actionIdForKey`), `NEW_GAME_ACTION_ID = 'system:new-game'`(n 키),
   `data-player-action` 배선(`wirePlayerActionButtons`). 액션 id 문자열
   (`choice:*`, `move:*`, `use:*`)은 core 계약이므로 렌더러가 변형하지 않는다.
4. **localStorage 키·값 불변**: `igu-hakji.rust.save.v1`,
   `igu-hakji.last-run-summary.v1`, `tui-adv.player-settings.v1`, legacy
   `escape-office.save.v1` / `escape-office.rust.save.v1`.
5. **새 npm 의존성 금지.** 이미 있는 `@fontsource/noto-serif-kr` 패키지에서
   weight css를 추가 import하는 것(예: `korean-900.css`)은 허용.
6. **QA 계약 셀렉터 유지** — `web/scripts/storybook-reference-qa.mjs`와
   `render.test.ts`가 검사한다. 아래 셀렉터/속성은 리디자인 후에도 존재해야 한다:
   - `[data-renderer="web-storybook"]`, `.storybook-shell`
   - `.storybook-hud[data-region="status"]`, `.story-progress-rail`
   - `[data-region="visual"]`, `[data-region="body"]`, `[data-region="choices"]`, `[data-region="history"]`
   - `.storybook-dock`, `button.choice-row[data-action-id]`, `.choice-bullet`
   - `[data-player-screen="start"]`, `[data-player-action="new-game"]`
   - 오류 패널 클래스 `.storybook-runtime-warning`, `.storybook-fatal`
   - 금지 유지: `.fake-tui`, `.storybook-topline`, 가시 텍스트 `CURRENT ENCOUNTER`/`LOCAL STATUS`
7. **GlyphFX/접근성**: `stable_terms`와 `fallback_text`는 reduced-motion,
   no-canvas에서도 항상 읽혀야 한다. `prefers-reduced-motion` 존중 블록 유지.
   색만으로 정보를 전달하지 않는다. 터치 타깃 최소 44px. 선택지 번호 고정.
8. **콘텐츠 데이터 불변**: 어떤 `.bundle.json`, `.yaml`, generated 파일도 수정 금지.
9. **테스트는 각 WP에 "수정 허용"으로 명시된 것만** 수정한다. 명시 없는 테스트가
   깨지면 코드가 잘못된 것이다.
10. **`STORYPACK_PREVIEW_OPTIONS = []`는 의도된 빈 배열** — 건드리지 않는다
    (`contentBundles.test.ts:75`가 검증).

### 0.2 검증 명령 (모든 WP 후 실행)

```bash
cd web && npm test && npx tsc --noEmit && npm run build && cd ..
```

Phase V의 시각 WP 후에는 추가로 수동/구조 QA:

```bash
# wasm이 없으면 1회: cd web && npm run wasm:build
cd web && npm run preview:wasm &   # 또는 별도 터미널에서 preview 서버 유지
npm run qa:storybook:visual -- \
  --base-url http://127.0.0.1:4173/ \
  --out-dir /tmp/tui-adv-storybook-visual-qa --require-wasm
```

WP-0 후에는 `python3 -m pytest tests/ -q` (repo venv 사용:
`./.venv/bin/python -m pytest tests/ -q`).

수동 확인 뷰포트: 390x844, 414x896, 800x1440, wide desktop.
수동 플로우: 시작 → 새 모험 → choice 1회 → move 1회 → 전투 encounter →
(프리뷰 씨드 123 기준 `choice:grip_employee_badge` 즉시 엔딩 경로도 1회) → 엔딩 CTA → 재시작.

### 0.3 커밋 컨벤션

WP당 커밋 1개.

```
fix(web-ui): <설명> [WP-U<n>]      # Phase U
feat(web-ui): <설명> [WP-V<n>]     # Phase V
test(docs): <설명> [WP-0]
docs(design): <설명> [WP-V10]
```

---

## 1. 현재 상태 진단 (왜 이 플랜이 존재하나)

2026-07-11에 모바일 뷰포트(375x812)로 실제 플레이하며 확인한 결과.

### 비주얼 — "허접함"의 근본 원인

- **모든 wuxia 장면이 동일한 CSS 도형 그림 하나를 재사용한다.**
  [visualCatalog.ts](web/src/ui/storybook/visualCatalog.ts)의 `visualKinds` 맵에 6개 id만 등록되어 있고,
  `visualKindFromLayout`이 나머지 wuxia id 전부를 같은 `renderWuxiaVisual`
  (절벽+문+나그네 gradient/clip-path 블롭)로 보낸다. 무협 팩 encounter는 35개+.
- **전투 카드가 가짜 데이터를 표시한다.** `renderCombatVisual`의
  "거리 확보/위협 높음/체력바"는 core 상태와 무관한 하드코딩 장식이다.
  UI 신뢰를 깨는 최악 패턴.
- **초상은 CSS 도형, dock 아이콘은 유니코드 문자**(▧▣◈✎◒) — 참조 미감과 격차가 큼.
- **시작 화면 `web/public/assets/intro-main.png`는 타사 게임
  (Life in Adventure)의 타이틀 스크린 원본이다.** 로고 텍스트까지 박혀 있다.
  저작권 침해 + 잘못된 브랜딩. **반드시 제거·교체** (WP-V8).

### UX 결함 (미감과 무관)

| # | 결함 | 위치 |
|---|---|---|
| 1 | 같은 제목이 화면에 3번 반복 (HUD nameplate + hud-subtitle + body h1) | `render.ts` `renderHud`/`renderBody` |
| 2 | 업적이 원시 id로 노출 (`+ 업적: wuxia_first_arrival`) | `render.ts` `renderInlineResultLog` |
| 3 | 엔딩 페이지가 막다른 길 — "현재 실행할 수 있는 행동이 없다"만 표시, 재시작 버튼 없음 (모바일에서 n 키 없음) | `render.ts` `renderChoices` |
| 4 | 모바일 ≤560px에서 `.choice-index`/`.choice-cost`를 `display:none` — 1차 타깃에서 비용·번호 정보 소실 | `storybook.css` 1452행 부근 |
| 5 | ⚙ 메뉴 "포기하기"(`abandon-run`)가 확인 없이 세이브 즉시 삭제 | `main.ts` `playerActionHandlers` |
| 6 | dock 5개 중 4개(단서/업적/목표/가방)가 눌러도 아무 일 없는 장식. aria-label은 기능이 있는 척 함 | `render.ts` `renderBottomDock` |
| 7 | 이동(move)과 스토리 선택(choice)이 시각적으로 구분 안 됨 | `render.ts` `renderActionButton` |
| 8 | sr-only `figcaption`이 `visual: {원시 id}` — 스크린리더에 디버그 문자열 | `visualCatalog.ts` 각 render 함수 |

### 리팩터링 잔여물 (Step 2 리뷰에서 발견)

- `tests/test_docs_contract.py` 3곳(±L2265, L2321, L2370)이 삭제된
  `crates/escape-core/src/final_epilogue.rs` 경로를 읽어 pytest 실패.
- `crates/escape-core/src/final_epilogue/mod.rs:12`의
  `#[allow(unused_imports)] pub(crate) use types::MainEndingType;` — 실사용 확인 후 정리.

---

## 2. 디자인 언어: "수묵 천기록(水墨 天記錄)"

### 2.1 컨셉 한 줄

> 플레이어는 게임 UI를 보는 게 아니라, **자신에 대해 쓰이고 있는 오래된 기록책
> (천기록)을 넘겨 보고 있다.** 모든 그림은 그 책에 그려진 수묵 삽화다.

화면 구조는 `docs/design/Mobile_Ink_Storybook_UI.md`의 **서책 몰입형 + 정보
드로어** 문법을 따른다: 기본 화면은 folio 헤더 / 본문 흐름(제목→서사→삽화→
선택지) / 하단 각주 스트립뿐이고, 상세 정보(상태·소지품·업적·기록·메뉴)는
각주 스트립을 탭해 여는 바텀시트 드로어에 담는다. 고정 상단 HUD, 초상,
스탯 그리드, 아이콘 dock을 기본 화면에 두지 않는다. 본문 세로쓰기는 금지
(장식 한자 1~2자 제외) — 영어 로컬라이즈에서도 성립해야 한다. 표면 문법은
픽셀 게임 오브젝트가 아니라 **먹·한지·인장·판각 장식**이다.

### 2.2 색 토큰 (`:root`에 CSS 변수로 정의 — WP-V1)

```css
--paper:        #e9dfc6;   /* 한지 기본 */
--paper-deep:   #d9cfb4;   /* 한지 그늘 */
--paper-lit:    #f3ecd6;   /* 한지 밝은 면(이름표 등) */
--ink:          #20242b;   /* 진묵 — 제목/인물 실루엣 */
--ink-body:     #2e2a23;   /* 본문 먹 */
--ink-faded:    #5d5546;   /* 옅은 먹 — 보조 텍스트 */
--ink-wash:     rgba(46, 42, 35, 0.10); /* 먹 번짐/안개 */
--seal-red:     #9e3c3f;   /* 주인(朱印) — 인장/경고/위험 */
--seal-red-lit: #b8474b;
--jade:         #3f7f72;   /* 옥 — 회복/긍정 신호 */
--gold-leaf:    #b98f2e;   /* 금박 — 업적/보상 */
--line-soft:    rgba(70, 58, 40, 0.28);
--line-hard:    rgba(70, 58, 40, 0.55);
```

기존 변수명(`--paper`, `--ink`, `--stamp-red` 등)과 겹치는 것은 새 값으로
갱신하고, 사용처를 일괄 확인한다. 색은 위 팔레트 밖으로 나가지 않는다
(전환 연출의 일시적 필터 제외).

### 2.3 타이포그래피

- 본문: Noto Serif KR 400, `1.05~1.15rem`, `line-height: 1.85`, `word-break: keep-all`.
- 장면 제목: Noto Serif KR **900** (`@fontsource/noto-serif-kr/korean-900.css`
  import를 `main.ts` 상단에 추가), 크기 `clamp(1.5rem, 5vw, 2.2rem)`.
- 각주/메타(비용, 사유, 기록 시간): `0.78~0.86rem`, `--ink-faded`.
  모노스페이스 폰트는 "시스템 로그" 정체성이 필요한 곳(런타임 경고, seed 입력)에만 남기고
  스토리 표면에서는 제거한다.
- 장식 한자(인장·판심)는 **장식으로만** 쓰고(aria-hidden), 의미 전달 텍스트는 항상 한글.

### 2.4 장식 어휘 (ornament vocabulary)

| 요소 | 구현 |
|---|---|
| 쌍선(雙線) 구획 | `border-top: 3px double var(--line-hard)` 계열 |
| 능화문 divider | 가는 선 + 중앙 `✦`/`✥` 글리프 (기존 choice-separator 개선) |
| 인장(seal) | `--seal-red` 사각 + 흰 한자 1자, `transform: rotate(2~4deg)` |
| 판심(版心) 헤더 | HUD 중앙의 얇은 세로 라벨(장 표시) |
| 먹 번짐 | SVG `feGaussianBlur` + 저투명 ellipse (장면 삽화 내부에서만) |

### 2.5 수묵 장면 삽화 시스템 원칙

- 삽화는 전부 **인라인 SVG**로 저작한다. 외부 이미지/에셋/새 의존성 금지.
- 인물은 **먹 실루엣**(머리 원 + 붓선 몸통, `stroke-linecap: round`)으로만 그린다.
  이목구비를 그리려 하지 말 것 — 이 시스템의 품질 상한은 "절제된 실루엣"에서 나온다.
- 모든 장면은 3개 층(원경 far / 중경 mid / 근경 near) + 안개 + 인물 + 인장으로 조립한다.
- **결정론**: 같은 `visual.id`는 항상 같은 그림. 변주는 `Math.random()`이 아니라
  id 문자열 해시(FNV-1a)로 시드된 지터만 허용 (UI_Rules "모든 왜곡 연출은 시드 제어" 준수).
- 애니메이션(먹이 스며드는 등장)은 CSS/SMIL 대신 CSS 트랜지션 + opacity로 하고
  reduced-motion에서 즉시 완성 상태를 보인다.

---

## 3. 워크패키지

실행 순서: **WP-0 → U1 → U2 → U3 → U4 → U5 → U6 → U7 → U8 → V1 → V2 → V3 → V4 → V5 → V6 → V7 → V8 → V9 → V10**.
어느 WP가 설명보다 위험해 보이면: 건너뛰고, 다음 커밋 메시지에 사유를 남기고 계속한다.

---

### WP-0: 리팩터링 잔여물 정리 (web 외 유일한 코드 수정)

1. `tests/test_docs_contract.py`에서 `crates/escape-core/src/final_epilogue.rs`를
   읽는 3곳(±L2265, L2321, L2370)을 연다. 각 테스트가 검사하는 내용을 읽고,
   WP-A1 분할 후의 올바른 파일로 경로를 바꾼다. 카드 본문 검사면
   `final_epilogue/cards.rs`, 룰 테이블 검사면 `final_epilogue/audit_rules.rs` 등 —
   **grep으로 실제 심볼이 어느 파일로 갔는지 확인 후** 교체한다. 필요하면 여러
   파일을 읽어 이어붙이는 방식으로 바꾸되 assert 내용 자체는 바꾸지 않는다.
2. `grep -rn "MainEndingType" crates/escape-core/src --include=*.rs` 실행.
   `final_epilogue/` 밖에서 사용처가 없으면 `mod.rs:12`의
   `#[allow(unused_imports)] pub(crate) use types::MainEndingType;`를 삭제하고
   내부 사용처 import를 정리한다. 사용처가 있으면 allow만 제거해 본다.
3. 검증: `cargo test --workspace` + `./.venv/bin/python -m pytest tests/ -q`
   (baseline에서 실패하던 docs-contract 3건이 통과로 바뀌고, 그 외 결과 동일해야 함).

---

## Phase U — UX 결함 수정

Phase U에서는 시각 스타일을 바꾸지 않는다. 최소 diff로 동작만 고친다.

### WP-U1: 제목 3중 반복 제거

**현상**: encounter 페이지에서 `hud-nameplate`(location.name), `hud-subtitle`(page.title),
body `<h1>`(page.title)이 겹쳐 같은/유사 문자열이 3번 보인다.

**규칙(이대로 구현)**:
- `hud-nameplate` = `page.location.name` (유지).
- `hud-subtitle`은 **삭제**한다. 장면 제목의 자리는 body `<h1>` 하나다.
- body `<h1>` = `page.title` (유지). 단, `page.title === page.location.name`인
  movement 페이지에서는 h1을 생략하고 본문만 둔다.

**수정 파일**: `web/src/ui/storybook/render.ts` (`renderHud`, `renderBody`).
**테스트 수정 허용**: `render.test.ts`에서 `hud-subtitle` 관련이 아닌 assert는
그대로 두어야 한다(현재 subtitle 전용 assert 없음 — `복합기가 혼자 출력한다`와
`복합기 구역` 포함 assert는 계속 통과해야 한다).

### WP-U2: 업적·아이템 원시 id 노출 제거

1. 새 파일 `web/src/ui/storybook/labels.ts`:
   ```ts
   export function achievementLabel(id: string): string { ... }
   export function inventoryItemLabel(id: string): string { ... }
   ```
   내부에 `Record<string, string>` 한국어 라벨 맵을 둔다. 라벨 원문은
   `docs/content/encounter_db/wuxia_jianghu_pack.md`와 번들에서 수집한다.
   번들의 업적/아이템 id 전수 목록은:
   ```bash
   python3 - <<'EOF'
   import json, re
   t = open('web/src/data/generated/storypack-preview/wuxia_jianghu_pack.content.bundle.json', encoding='utf-8').read()
   b = json.loads(t)
   text = json.dumps(b, ensure_ascii=False)
   print(sorted(set(re.findall(r'"(wuxia_[a-z0-9_]+)"', text))))
   EOF
   ```
   에서 achievement/item 컨텍스트를 확인해 작성한다 (번들 구조를 읽고
   achievements/items 배열 위치를 파악할 것).
2. 맵에 없는 id의 폴백: `이름 없는 기록` 같은 뭉개기 금지. id의 밑줄을 공백으로
   바꾼 문자열을 그대로 보여주되 `<small>` 각주로 표기해 미번역임이 드러나게 한다.
3. `render.ts` `renderInlineResultLog`와 `renderBottomDock`(업적 개수), 추후
   WP-U6 drawer에서 이 함수만 통해 표기한다.

**주의**: 라벨 맵은 렌더러-로컬 표기 사전이다. core가 라벨을 주는 구조 변경
(ScenePage 필드 추가)은 이번 범위 밖 — invariant 2.

### WP-U3: 엔딩·막다른 페이지에 다음 행동 제공

`render.ts` `renderChoices`에서 `actions.length === 0`일 때 현재는
`empty-choice` 문구만 렌더한다. 다음으로 교체:

```html
<li class="empty-choice">…기존 문구 유지…</li>
<li><button class="choice-row" data-player-action="show-start">
  <span class="choice-bullet" aria-hidden="true">✥</span>
  <span class="choice-label">처음 화면으로 돌아간다</span>
</button></li>
```

- `data-action-id`가 아니라 `data-player-action`을 쓴다 (core 액션이 아니므로).
  `main.ts` `renderGamePage`가 이미 `wirePlayerActionButtons(appRoot)`를 호출하므로
  추가 배선은 불필요 — 확인만 할 것.
- `page.mode === 'ending'`이면 empty 문구 없이 버튼만, 문구는
  `기록의 이 장은 여기서 끝났다.`로 교체.
- 키보드 n(새 모험) 동작은 그대로 유지.

**테스트 수정 허용**: `render.test.ts` L217의
`expect(html).toContain('현재 실행할 수 있는 행동이 없다.')`는 이 WP 이후
ending 모드에서 문구가 바뀌므로 — 해당 테스트의 페이지는 `mode: 'ending'`이다 —
`기록의 이 장은 여기서 끝났다.`와 `data-player-action="show-start"` 포함 assert로 교체.

### WP-U4: 모바일에서 선택지 번호·비용 숨김 해제

`storybook.css`의 `@media (max-width: 560px)` 블록에서
`.choice-index, .choice-cost { display: none; }` 규칙을 삭제한다.
좁은 폭에서 넘치지 않도록 `.choice-row`의 mobile grid를
`auto auto minmax(0,1fr)` 3열로 하고 cost는 라벨 아래 줄로 내린다
(`.choice-cost { grid-column: 3; }` 방식). 44px 터치 타깃 유지.

### WP-U5: "포기하기" 확인 단계

`main.ts` `playerActionHandlers`의 `abandon-run`은 즉시
`clearPlayerSaves`를 호출한다. 2단계로 바꾼다:

1. 새 모듈 상태 `let confirmAbandon = false;` 추가.
2. `abandon-run` 핸들러: `confirmAbandon = true; render();`로 변경.
3. 새 핸들러 `confirm-abandon-run`(기존 삭제+초기화 로직 이동),
   `cancel-abandon-run`(`confirmAbandon = false; render();`).
4. `renderGamePage`에서 `confirmAbandon`이면 본문 위에 확인 패널
   (`.storybook-confirm` — `start-reset-confirmation`과 같은 문법)을 prepend:
   문구 `이 모험의 기록을 지우고 처음으로 돌아갈까요?`,
   버튼 `기록을 지우고 돌아간다` / `계속 모험한다`.
5. storypack preview 중이면(세이브를 지우지 않는 경로) 확인 없이 기존처럼 즉시 이동.

`render.test.ts`의 `data-player-action="abandon-run"` assert는 계속 통과해야
한다(메뉴 버튼 자체는 유지되므로).

### WP-U6: dock 정직화 — 가방·업적 drawer 구현, 단서·목표 정리

> Rev 2 참고: 여기서 만드는 개별 drawer들은 WP-V7에서 통합 정보 드로어의
> 섹션으로 흡수된다. U 단계에서는 기존 dock 구조 위에서 최소 구현만 한다.

`render.ts` `renderBottomDock` 재작성:

1. **기록**: 현행 유지 (`href="#story-history"` → history `<details>`).
2. **가방**: `<details class="dock-drawer">` + `<summary>` 패턴으로 교체.
   내용은 `page.inventory_summary.items`를 `inventoryItemLabel`(WP-U2)로 나열,
   `overflow_count > 0`이면 `…외 N개` 행 추가, 비어 있으면 `아직 지닌 것이 없다.`.
3. **업적**: 같은 패턴. `achievement_summary.unlocked`를 `achievementLabel`로 나열,
   `newly_unlocked`는 `new` 표식(`--gold-leaf` 점 + `새로 새김` sr 텍스트).
4. **단서/목표**: 이번 슬라이스에서 데이터 소스가 없으므로 **dock에서 제거**한다.
   자리 유지용 가짜 버튼을 남기지 말 것.
5. aria-label을 실제 동작과 일치시킨다 (`aria-label="기록"`, `aria-label="소지품"`은
   테스트가 확인하므로 유지).

drawer가 열릴 때 페이지 재렌더로 닫히는 문제(전체 innerHTML 교체 구조)는
이번엔 감수한다 — 액션 실행 시 닫히는 게 오히려 자연스럽다.

**테스트 수정 허용**: `render.test.ts`에 dock 관련 신규 assert 추가는 자유.
기존 `aria-label="기록"`/`aria-label="소지품"` assert는 유지되어야 한다.

### WP-U7: 이동/선택 액션 시각 구분

`renderActionButton`에서 `action.kind`에 따라 bullet 글리프를 분기:
`choice` → `✥`, `move` → `➤`(또는 `〉`), `use` → `◈`. bullet `<span>`에
`data-bullet-kind` 속성을 추가하고 CSS에서 move 계열은 `--ink-faded` 색으로.
move 액션이 2개 이상이고 choice와 섞여 있으면 move 그룹 앞에
`<li class="choice-group-label" aria-hidden="true">이동</li>` 캡션 행을 넣는다.
번호 체계(전체 액션 순서 = 숫자키)는 절대 바꾸지 않는다 — 그룹 캡션은
`<li>`이되 번호를 소비하지 않는 비버튼 요소다.

### WP-U8: sr-only 원시 id 캡션 제거

`visualCatalog.ts`의 모든 `<figcaption>visual: ${id}</figcaption>`을
`<figcaption>${escapeHtml(visual.alt)}</figcaption>`로 교체. alt가 빈 문자열이면
figcaption 생략. (WP-V2에서 이 파일이 대체되지만, U 단계에서 먼저 고쳐 두면
V2 지연 시에도 사용자 피해가 없다.)

---

## Phase V — 수묵 리디자인

### WP-V1: 디자인 토큰·타이포·베이스 스타일

1. `main.ts` 상단에 `import '@fontsource/noto-serif-kr/korean-900.css';` 추가.
2. `storybook.css`의 `:root` 팔레트를 §2.2 토큰으로 교체·정리한다.
   기존 변수를 참조하는 모든 셀렉터를 새 토큰으로 마이그레이션 (기계적 치환 후
   시각 확인). `--terminal-green`/`--clue-cyan` 같은 오피스-톤 변수는
   `--jade`로 통합. 시스템 로그 성격(런타임 경고)만 기존 톤 유지.
3. 본문/제목 타이포를 §2.3대로 갱신 (`.storybook-body h1`, `.storybook-body p`).
4. `.storybook-shell` 종이 배경은 유지하되 새 토큰 기준으로 재조정. 다크 배경
   카드(`.message-card`, `.corridor-card`, `.placeholder-card`의 어두운 gradient)는
   이 WP에서 제거하지 말고 WP-V2에서 삽화 시스템과 함께 대체한다.
5. 능화문 divider 공통 클래스 `.ink-rule` 추가 (choice-separator가 사용하도록).

**검증**: 0.2 기본 + 수동 육안 (텍스트 대비: 본문 `--ink-body` on `--paper`는
대비 10:1 이상이므로 안전. 각주 `--ink-faded`는 4.5:1 이상인지 확인).

### WP-V2: 수묵 장면 삽화 시스템 (이 플랜의 핵심)

**새 파일 4개** — `web/src/ui/storybook/ink/`:

```
inkSpec.ts        // 타입 + FNV-1a 해시 + 시드 지터 유틸
inkPrimitives.ts  // SVG 조각 생성기 (순수 함수: spec 조각 → SVG string)
inkScenes.ts      // visual id → InkSceneSpec 테이블 + 모드별 generic spec
renderInkVisual.ts// ScenePage.visual + effect_cues → <figure> HTML
```

`render.ts`는 `renderVisualCard`(visualCatalog) 호출을
`renderInkVisual`로 교체하고, `visualCatalog.ts`는 삭제한다
(printer/messenger/corridor 등 오피스 번들 시각도 ink generic으로 수렴 —
오피스 번들은 현재 웹 기본 경로가 아니다).

#### inkSpec.ts

```ts
export type InkElementKind =
  | 'ridge' | 'roofline' | 'gate' | 'pine' | 'bamboo' | 'road'
  | 'wall' | 'stairs' | 'desk' | 'lantern' | 'campfire' | 'banner'
  | 'scroll' | 'rain' | 'moon';

export interface InkElement { kind: InkElementKind; x: number; scale?: number; flip?: boolean; }

export type InkPose =
  | 'stand' | 'walk' | 'confront' | 'kneel' | 'fallen' | 'bow'
  | 'duel-left' | 'duel-right' | 'sit' | 'reach';

export interface InkFigure { pose: InkPose; x: number; scale?: number; weapon?: 'sword' | 'club' | 'staff' | 'none'; }

export interface InkSceneSpec {
  horizon: number;                   // 0..1, 지평선 높이
  mist: 0 | 1 | 2 | 3;               // 안개 농도
  far?: InkElement[];
  mid?: InkElement[];
  near?: InkElement[];
  figures?: InkFigure[];
  seal?: string;                     // 인장 한자 1자 (aria-hidden 장식)
  accent?: 'none' | 'seal-red' | 'gold';
  night?: boolean;                   // 야경: 먹 농도 반전 없이 wash만 짙게
}

export function fnv1a(text: string): number { /* 표준 FNV-1a 32bit */ }
export function jitter(seed: number, index: number, range: number): number { /* 결정론 지터 */ }
```

#### inkPrimitives.ts

각 `InkElementKind`/`InkPose`마다 SVG 문자열을 반환하는 순수 함수.
스타일 규칙:

- viewBox `0 0 280 168` 고정. 층별 불투명도: far 0.30~0.40, mid 0.55~0.65,
  near/figures 0.9~1.0. 색은 전부 `--ink` 계열 currentColor/고정 hex
  (`#20242b`, `#3a352b`) — SVG 내부는 토큰 상속이 안 되므로 hex 직접 사용.
- 붓선: `stroke-linecap="round"`, 폭 2.5~9로 강약. 인물은
  "머리 `<circle r=7~8>` + 몸통/팔다리 Q-곡선 stroke" 문법으로 §2.5 원칙 준수.
- 안개: `feGaussianBlur stdDeviation=4` 필터 1개를 `<defs>`에 두고
  타원 1~3개 (mist 레벨만큼).
- 인장: 우하단 `<rect>` `--seal-red`(#9e3c3f) + 흰 세리프 한자, rotate 2~4deg.
- 지터: 요소 x/scale에 `jitter(fnv1a(visualId), i, 0.04)` 적용 — 같은 id는 항상 같은 그림.

#### inkScenes.ts — 장면 테이블

아래 표를 `Record<string, InkSceneSpec>`으로 구현한다. **구현 전에 각 id의
encounter 제목/본문을 번들에서 실제로 읽고**, 어긋나는 구도는 본문에 맞게 조정한다
(표가 스펙이 아니라 본문이 스펙이다). 표기: 원경/중경/인물/인장.

| visual id | 구도 지시 | 인장 |
|---|---|---|
| `wuxia_commute_rift` | 원경 능선+안개3, 중경 관도(road), 인물: stand 1(가방=reach 없이 stand) — 낯선 길 한복판의 출근자 | 裂 |
| `wuxia_heuksa_bang_first_fight` | 중경 지붕선+banner, 인물: confront(club) vs stand, 근경 흙길 | 難 |
| `wuxia_cheonggi_record_first_fragment` | 중경 desk+scroll+lantern, 인물: sit 1, 안개1 | 記 |
| `wuxia_seo_harin_rescue` | 중경 wall+초옥 roofline, 인물: kneel + fallen, 안개2 | 救 |
| `wuxia_cheongryu_apprentice_entry` | 원경 산문(gate)+stairs+pine, 인물: bow 1, 안개2 | 門 |
| `wuxia_cheongryu_chore_sparring` | 중경 연무장 wall, 인물: duel-left(staff) vs duel-right(staff) | 修 |
| `wuxia_cheongryu_raid_route_split` | 갈림길 road 2방향, 원경 연기(mist3)+banner, 인물: stand 2 | 岐 |
| `wuxia_cheongryu_raid_wounded_fallback` | 근경 wall 붕괴선, 인물: walk + fallen 부축, night | 退 |
| `wuxia_baekdo_medicine_debt` | 중경 초옥 roofline+lantern, 인물: kneel + stand, 안개1 | 藥 |
| `wuxia_black_heaven_escape_price` | night, 중경 wall+moon, 인물: walk 1(빠른 붓선), 안개2 | 逃 |
| `wuxia_heavenly_archive_previous_outsiders` | 중경 서가(wall+scroll 다수)+lantern, 인물: reach 1 | 藏 |
| `wuxia_wounded_shelter_dawn_offers` | 근경 campfire, 원경 능선 여명(horizon 낮게), 인물: sit 2 | 曉 |
| `wuxia_mumyeong_first_sighting` | 원경 능선, 중경 대나무(bamboo), 인물: stand(원거리 소형 실루엣 1) | 影 |
| `wuxia_mumyeong_first_confrontation` | 인물: confront(sword) vs stand, 안개2, 근경 road | 問 |
| `wuxia_mumyeong_copy_style_reveal` | 인물: duel-left vs duel-right — **같은 자세 좌우 대칭**(flip), 안개1 | 倣 |
| `wuxia_cheonoe_pyeonrin_first_reward` / `_second_reward` | 중경 desk+scroll, gold accent, 인물: reach 1 | 賞 |
| `wuxia_mumyeong_reads_orthodox_style` | 중경 연무장, 인물: sit(관찰) + duel-right, 안개1 | 讀 |
| `wuxia_mumyeong_midgame_reunion` | 중경 road+pine, 인물: stand 2 마주봄(confront 아님 — 무기 none) | 逢 |
| `wuxia_cheonggi_record_writing_sense` | desk+scroll+lantern 근경 확대, 인물 없음, 안개1 | 書 |
| `wuxia_boss_first_appearance` | 원경 높은 wall/stairs 위 stand 1(소형), 근경 banner, seal-red accent | 王 |
| `wuxia_mumyeong_request_for_aid` | night, campfire, 인물: stand + kneel | 請 |
| `wuxia_mumyeong_awakening` | 인물: stand 1 중앙, 주위 mist3 소용돌이(taper 곡선), seal-red accent | 覺 |
| `wuxia_qingliu_attack_after_war` | 중경 gate 파손+연기 mist3, 인물: confront 2 | 襲 |
| `wuxia_mumyeong_destroys_orthodox_sect` | night, roofline 화염 대신 짙은 wash, fallen 다수(2), stand 1 | 滅 |
| `wuxia_boss_recruits_mumyeong` | 인물: stand(높은 단 위) + stand(아래), banner, seal-red | 招 |
| `wuxia_mumyeong_departure_truth_summary` | 원경 road 소실점, 인물: walk 1 뒷모습(소형), 안개2 | 去 |
| `wuxia_seoharin_empty_place` | 중경 초옥+desk, **인물 없음**, 소품만(scroll, lantern 꺼짐), 안개2 | 空 |
| `wuxia_seoharin_left_meal` | 근경 desk 위 그릇(원 2개), 인물 없음, 안개1 | 溫 |
| `wuxia_sado_final_battle` / `_phase_1~3` | duel-left(sword) vs duel-right(sword), 원경 능선, mist는 phase마다 1→3 증가, phase3는 seal-red accent | 決 |
| `wuxia_sado_battle_loss_route_bridge` | fallen 1 + stand 1(멀어짐), night | 敗 |
| `wuxia_boss_resolution` / `wuxia_mumyeong_resolution` / `wuxia_seoharin_qingliu_resolution` / `wuxia_cheongirok_resolution` | 결산 장면: desk+scroll 중심, 인물 stand 1, gold accent | 結 |
| `ending:wuxia_return_modern_commute_scene*` / `wuxia_returned_commute` | 원경 능선이 **빌딩 스카이라인(wall 직선화)으로 반쯤 전이**, 인물 stand 1 | 歸 |
| `ending:wuxia_settlement_stay_scene*` / `wuxia_qingliu_settlement` | 초옥+pine+밥짓는 연기(mist 세로), 인물 sit 2 | 定 |
| `wuxia_black_serpent_aftermath` | 찢어진 banner + 빈 장터 roofline, 인물 없음 | 痕 |
| `ending:wuxia_preview_grounded` / `wuxia_preview` | 관도+사원증 모티프(근경 사각 카드 + 끈 곡선), 인물 stand 1 | 存 |
| `location:*` (이동 페이지 전부) | location id 해시로 ridge/roofline/gate/bamboo 중 2개 조합 + road, 인물 walk 1 | 行 |
| `storypack:*` (로딩) | 빈 종이 + 벼루(desk 축소) + mist1, 인장 없음 | — |

엔딩 페이지의 `visual.id`는 `ending:<scene_id>` 형태로 온다(실플레이 확인됨).
테이블 키는 `ending:` 접두사 포함 원문 id로 넣거나, lookup에서 접두사를
벗겨 2차 조회한다 — 구현에서 한 가지 방식을 정해 주석으로 남길 것.

**generic 폴백** (테이블 미등록 id): `mode`별 기본 spec
(encounter → confront 구도 / movement → road+walk / ending → desk+scroll)에
해시 지터를 적용. 이때 `<figure>`의 `data-visual-kind="placeholder"`를 유지하고
`visual.alt`를 눈에 보이는 각주 캡션으로 렌더한다 —
`render.test.ts`의 unknown-visual 테스트(placeholder kind + alt 텍스트 + 액션 보존)가
**수정 없이 통과해야 한다.**

**`<figure>` 계약 유지**: `class="storybook-visual"`, `data-region="visual"`,
`data-visual-id`, `data-visual-kind` 유지. GlyphFX cue 블록(`renderGlyphFxCues`
로직)은 `renderInkVisual.ts`로 이식하고 스타일만 §2 토큰으로 (stable terms
`<mark>` 가독성 필수). `printer-flow` canvas 훅(`data-anomaly-canvas`)은 오피스
번들 전용이므로 유지하되 ink 시스템과 무관하게 남겨 둔다 (`printerFlow.test.ts` 보존).

**CSS**: `.wuxia-card`, `.combat-card`(V3에서), `.message-card`,
`.corridor-card`, `.placeholder-card`, `.pixel-illustration` 등 CSS-art 클래스
일괄 삭제, `.ink-scene` 계열로 대체. `image-rendering: pixelated` 제거.

**테스트 수정 허용**: `render.test.ts`의 시각 관련 assert 중
`data-visual-kind="combat"`(V3에서 처리), CSS-art 클래스명을 확인하는 것.
`data-region="visual"`/placeholder 테스트/GlyphFX 테스트는 수정 없이 통과할 것.

### WP-V3: 전투 장면 정직화

1. `renderInkVisual`에서 combat 계열(`visual.kind === 'combat_intervention'` 또는
   id가 fight/brawl/duel 포함)은 duel 구도 spec을 쓰되, **하드코딩 수치·상태
   문구("거리 확보", "위협 높음", 가짜 게이지, "전투 발생/상황 개입")를 전부 제거**한다.
2. 전투 페이지에서 코어가 실제로 주는 것만 표기: `status_summary.danger`(위험 띠),
   `pressure_cues`, 본문/선택지. 삽화 아래 한 줄 각주로
   `page.visual.alt`를 표시한다 (전투 상황 설명은 alt와 본문이 담당).
3. `data-story-phase="combat"` 판정(`isCombatScene`)은 유지 — 배경 톤 분기용.

**테스트 수정 허용**: `render.test.ts` combat 테스트의
`'전투 발생'`, `'상황 개입'`, `'선택지로 거리, 균형, 도주로를 고른다'`,
`combat-card` assert를 새 구조(예: `data-visual-kind="combat"` +
`.ink-scene--duel` + alt 텍스트)로 교체.

### WP-V4 (Rev 2): 각주 스트립 — 기본 화면에서 HUD 해체

`renderHud`를 **하단 고정 각주 스트립(footnote strip)** 렌더러로 재작성한다.
`class="storybook-hud"`와 `data-region="status"`, `data-danger-band`는 유지하되
(QA 계약), 위치는 화면 하단 sticky로 옮기고 내용을 다음으로 축소한다:

1. **쪽 표시**: `documentLabel(page)` 결과 + turn (예: `천기록 · 3쪽`).
2. **몸/마음 요약**: 각 5개 먹점 `●`(채움, --ink) `◐`(반) `○`(빈, --ink-faded).
   `data-band`별 강조는 점 아래 가는 밑줄 색으로 (색+형태 이중 부호화).
   aria-label 규칙(`라벨 텍스트 값`)과 `data-resource-id` 유지.
3. **위험 실**: WP-V5의 `story-progress-rail`을 스트립 안에 통합.
4. **드로어 토글 버튼**: `aria-expanded` + `aria-controls`로 WP-V7 드로어 개폐.

기본 화면에서 **초상, 이름표(nameplate), 스탯 그리드, ⚙ hud-menu를 제거**한다
— 상세 상태와 메뉴는 전부 WP-V7 드로어로 이동한다. `page.location.name`은
본문 위 folio 헤더의 작은 각주로 옮긴다(제목은 body h1 하나 — WP-U1 규칙 유지).
숫자 노출은 중단하고 `resource.text`(코어 band 문구)를 주 표기로, 숫자는
aria-label/`title`에만 남긴다. `status_summary.warnings`와 `pressure_cues`는
스트립이 아니라 **본문에 붉은 각주 블록**으로 삽입한다.

**테스트 수정 허용**: `render.test.ts`의 `hud-nameplate`/`hud-menu`/
`hud-stat-grid` 클래스 assert를 새 구조(각주 스트립 + 드로어) assert로 교체.
`class="storybook-hud"`, `data-region="status"`, `not.toContain('단말기 전원')`
계열 assert는 유지되어야 한다. `data-player-action="show-start"`/`"abandon-run"`/
`"toggle-audio"` assert는 렌더 결과 어딘가(드로어 내부)에 존재하면 통과한다.

### WP-V5 (Rev 2): 위험 실 — 각주 스트립 통합

`renderProgressRail`의 출력을 독립 상단 rail에서 **각주 스트립 내부 요소**로
옮긴다. 스타일: 한지 위에 걸린 **붉은 실 한 가닥**(가는 `--seal-red` 선) —
danger band에 따라 실 굵기·요동(약한 kink 곡선)을 단계화. `--progress` 변수,
`class="story-progress-rail"`, `data-danger-band`, aria-label 계약 유지.
진행률로 오독되는 fill 게이지 형태는 금지 (위험 압박 표현이라는 원 계약 준수).

### WP-V6: 선택지 영역 재스킨

- `choice-separator` → `.ink-rule` (능화문: 가는선—✦—가는선).
- `.choice-row` hover/focus: 현재 text-shadow 글로우 제거 →
  라벨 아래 **먹 번짐 밑줄**(pseudo-element, opacity transition 120ms)과
  bullet 색 전환(`--seal-red`). focus-visible에서 outline 완전 제거 금지 —
  `outline: 2px solid var(--seal-red); outline-offset: 4px` 부여.
- `.choice-index`: 괄호 한자식 번호가 아닌 현행 숫자 유지(숫자키 계약 가시화).
  스타일만 각주 톤.
- blocked action: 회색 처리 + 사유는 현행 유지, bullet `✧`.

### WP-V7 (Rev 2): 정보 드로어 — dock을 바텀시트로 통합

`renderBottomDock`을 **바텀시트 정보 드로어** 렌더러로 재작성한다.
`class="storybook-dock"`은 드로어 컨테이너에 유지한다 (QA 계약). 개폐 트리거는
WP-V4의 각주 스트립 토글 버튼이며, 닫힌 상태에서는 화면에 아이콘 바를 남기지
않는다. JS 상태 없이 `<details>` 또는 `data-drawer-open` 속성 + CSS로 구현
(전체 innerHTML 재렌더 구조와 충돌하지 않게).

드로어 내용 (섹션 순서 고정):

1. **상태 상세**: 몸/마음/위험의 `label + text`(코어 band 문구) 목록.
   숫자는 aria/`title`에만.
2. **소지품**: `inventory_summary.items` → `inventoryItemLabel`(WP-U2),
   overflow는 `…외 N개`, 비면 `아직 지닌 것이 없다.` — `aria-label="소지품"` 유지.
3. **업적**: `achievementLabel` 목록, `newly_unlocked`는 `--gold-leaf` 점 +
   `새로 새김` sr 텍스트 — `aria-label="업적"` 유지.
4. **기록**: `renderStoryHistory` 출력(`data-region="history"`)을 드로어 안으로
   이동 — `aria-label="기록"` 유지.
5. **메뉴**: 기존 ⚙ hud-menu의 버튼들을 이곳으로 통합 — `show-start`,
   `abandon-run`(WP-U5 확인 단계 유지), `toggle-audio`, `cycle-motion`.
   `data-player-action` 값 불변.

WP-U6에서 만든 dock 개별 drawer들은 이 통합 드로어의 섹션으로 흡수한다
(U6의 산출물 중 라벨 함수와 "단서/목표 제거" 결정은 그대로 유효).
각 인터랙티브 요소 터치 타깃 44px 이상. 섹션 제목은 장식 한자 1자
(狀/囊/勳/冊/器, aria-hidden) + 한글 라벨.

**테스트 수정 허용**: `render.test.ts`의 dock 관련 assert를 드로어 구조로 교체.
`aria-label="기록"`/`aria-label="소지품"`, `data-region="history"`,
`data-player-action` 계열 assert는 존재 위치만 바뀌고 유지되어야 한다.

### WP-V8: 시작 화면 저작 (Rev 2: 저작권 파일은 선행 제거됨)

> Rev 2 선행 커밋에서 `intro-main.png` 삭제, `<img>` 제거, 로고 문구를
> `이구학지 / 天記錄 — 천기록`으로 교체 완료. 이 WP에서는 수묵 타이틀 씬
> 저작과 메뉴 재스킨만 남는다.

1. `startScreen.ts`의 hero에 인라인 SVG 수묵 타이틀 씬 추가
   (`viewBox 0 0 390 700` 세로 구도: 원경 협곡 능선 + 안개, 중경 산문(gate)과
   돌계단, 근경 출근복 나그네 실루엣 1 — inkPrimitives 재사용 가능하면 재사용).
2. 로고 락업 스타일: 제목 `이구학지`(Noto Serif KR 900) + 부제
   `天記錄 — 천기록` + 인장 1개. 제목 세로쓰기는 장식으로만 허용하되,
   구현이 번거로우면 가로쓰기로 — 본문 세로쓰기 금지 원칙과 일관되게.
3. `TAP TO START` → `천기록을 연다` (버튼 크기·위치 유지, 클래스
   `start-tap-button`과 `data-player-action="open-start-menu"` 유지,
   `startScreen.test.ts`의 해당 assert 갱신 허용).
3. start-menu-panel 재스킨: §2 토큰 + 쌍선/인장 문법. 버튼 라벨(이어하기/새 모험/
   기록 삭제)과 `data-player-action` 값, seed input(`name="seed"`)은 불변 —
   `main.ts`의 `seedFromStartInput`과 `startScreen.test.ts`가 의존한다.
4. `data-player-screen="start"`, `data-start-menu-open` 계약 유지.

**테스트**: `startScreen.test.ts`는 저장/요약 로직 중심이라 무수정 통과가
목표. 마크업 assert가 있으면 그 항목에 한해 수정 허용(문구 교체 반영).

**QA 스크립트 수정 (이 WP에 포함)**: `storybook-reference-qa.mjs`
`maybeStartPlayer`는 숨겨진 `[data-player-action="new-game"]`을 바로 클릭한다.
drawer가 닫혀 있으면 실패하므로: `.start-tap-button`이 보이면 먼저 클릭 →
그 다음 new-game 클릭으로 수정한다.

### WP-V9: 전환 연출 — 먹 번짐

`transitionPlan.ts`/`transitionController.ts`는 **수정 금지**
(transition name 문자열이 계약: `start-fade`, `paper-slide`, `ink-pulse`,
`danger-glitch`, `ending-fade`, `paper-fade`). `storybook.css`의 해당
keyframes/트랜지션 스타일만 교체한다:

- `ink-pulse`: 먹이 스며드는 느낌 — opacity + 미세 blur(1px 이하) + contrast.
- `danger-glitch`: 디지털 글리치 skew 대신 **주묵이 튀는** 느낌 —
  `--seal-red` 오버레이 플래시(기존 `data-transition-danger` after 요소 재활용).
- `ending-fade`: 세피아 → 먹이 마르는 톤(saturate 감소)으로.
- 지속시간 180~320ms 현행 유지, `prefers-reduced-motion` 블록 유지.

`transitionPlan.test.ts`/`transitionController.test.ts` 무수정 통과 필수.

### WP-V10 (Rev 2): 설계 문서 마감 확인

문서 개정은 Rev 2 선행 커밋에서 완료되었다
(`docs/design/Mobile_Ink_Storybook_UI.md` 신설, 구 픽셀 문서 supersede,
README/00_Index/Development_Plan/UI_Rules 갱신, docs-contract 테스트 갱신,
참조 이미지 제거). 이 WP에서 남는 일:

1. 전체 구현 완료 후 `docs/design/Mobile_Ink_Storybook_UI.md`의
   Acceptance checklist를 실물과 대조해 어긋난 항목이 있으면 구현을 고친다
   (문서를 구현에 맞추지 말 것).
2. `./.venv/bin/python -m pytest tests/test_docs_contract.py tests/test_web_visual_qa_contract.py -q` 통과 확인.

---

## 4. 명시적 범위 밖 (하지 말 것)

- `ScenePage`/wasm JSON 경계에 필드 추가 (visual composition을 core로 옮기는
  작업은 별도 스키마 설계 후).
- 진짜 픽셀아트/외부 이미지 에셋 도입, 이미지 파일 추가.
- 오디오 리디자인 (`audioEngine`는 현행 유지, 라벨 문구만 필요 시).
- 터미널 렌더러(escape-terminal) 시각 변경.
- 새 콘텐츠/밸런스/엔딩 작업, 오피스 번들 전용 시각 복원.
- `main.ts`의 모듈 분리·상태 컨트롤러 도입 (여전히 테스트 없음 — diff 최소 원칙).
- tsconfig strictness 변경, 의존성 추가/업그레이드.

## 5. 최종 체크리스트

- [ ] WP당 커밋 1개, 지정 순서.
- [ ] `cd web && npm test && npx tsc --noEmit && npm run build` green.
- [ ] `cargo test --workspace` green (WP-0 영향 확인).
- [ ] `./.venv/bin/python -m pytest tests/ -q` — WP-0 이후 docs-contract 3건
      포함 전부 통과 (baseline 환경성 실패 제외).
- [ ] `npm run qa:storybook:visual -- --require-wasm ...` 전 뷰포트 통과.
- [ ] `git diff --stat main` 기준 변경 파일이 `web/`, `tests/test_docs_contract.py`,
      `crates/escape-core/src/final_epilogue/mod.rs`(선택), `docs/design/*`,
      `docs/00_Index.md` 밖에 없는지 확인. `web/src/data/generated/**` 무변경.
- [ ] `web/public/assets/intro-main.png`가 저장소에서 제거되었는지 확인.
- [ ] 수동 플로우(0.2) 완료: 시작→선택→이동→전투→엔딩→재시작, 뷰포트 4종.
- [ ] 완료/스킵 WP 목록과 편차를 `fable_ui_step2_report.md`(repo root)에 기록.
