# 절차적 전투 그래픽 — Codex 인수인계 플랜 (step1)

작성: 2026-08-12 · 작성자: Fable(Claude) · 인수: Codex

> **이 문서의 목적**
> Fable의 토큰이 소진되어 `AGENTS.md`의 「협업 루프」 조항대로 **플랜/설계 역할까지 Codex가 이어받는다.**
> 따라서 이 문서는 "구현 지시서"가 아니라 **Codex가 계획을 세울 수 있는 근거 묶음**이다.
> 확정된 것과 미확정인 것을 명시적으로 갈라 적었다. 미확정 항목은 Codex가 설계해야 한다.

## Baseline

- 브랜치: `claude/wave-racer-asset-analysis-gr3fh1`
- PR: **#209**
- Baseline 커밋: `620d31d` (docs(combat): define I7 transaction contract (#208))
- 이 트랙의 커밋: `e548052` → `cde65ef` → `9818e8f` → (이 문서)
- **게임 코드 경로 무변경.** `crates/escape-core/examples/` 1개 추가 외에 `crates/escape-core/src/`, `web/`, YAML, 생성 번들, 테스트 모두 그대로다. `prototypes/`는 배포되지 않는다.

---

## 0. 먼저 고쳐야 할 것 — `AGENTS.md`가 Codex를 잘못 인도한다

**`AGENTS.md:69`**, 「시각/게임필 방향 **(확정 결정, 임의 변경 금지)**」:

> **수묵 천기록**: 픽셀아트 흉내 폐기, SVG 먹 실루엣 + 한지 토큰만 사용. 신규 색상 리터럴 금지.

**이 항목은 폐기된 방향이다.** 사용자가 2026-08 세션에서 명시했다 — *"수묵, 먹선과 같은 컨셉은 버린지 오래인데 왜 아직 남아있는거지? 고려하지 말아줘."*

Codex는 `AGENTS.md`를 규약으로 읽으므로, 이 줄을 놔두면 **이 문서의 결론과 정면으로 어긋나는 설계를 하게 된다.**

왜 살아남았는지도 기록해 둔다 — 재발 방지에 필요하다:

- 아트 트랙이 **2026-07-17 이후 멈춰 있다** (`web/src/ui/storybook/ink|art`, `web/public/assets/art` 마지막 커밋 `5ab25cb`). 이후 약 50개 PR이 전부 combat/story/docs다.
- 먹선 SVG가 **폴백 경로**다. 등록 안 된 `visual_id`는 조용히 SVG로 떨어지고(번들에 `placeholder: true` 47개), 폴백은 터지지 않고 테스트를 깨지 않고 버그 리포트를 만들지 않는다. **그럴듯한 뭔가가 나오니까 아무도 눈치채지 못했다.**
- 방향이 바뀐 흔적은 문서 안에 이미 있었다. `docs/design/Mobile_Ink_Storybook_UI.md`의 91행("먹 실루엣 inline SVG로 저작")과 100행("**애니메이션풍** 일러스트 에셋")이 같은 문서에서 서로 모순이다.

**→ WP-0 참조. 사용자 확인 후 수정할 것.** (Fable은 "확정 결정, 임의 변경 금지"로 표시된 절을 임의로 고치지 않았다.)

---

## 1. 확정된 것 (측정으로 뒷받침됨 — 재론하지 말 것)

### 1-1. 4층 애니메이션 구조는 차원과 무관하다

INK TIDE(`Vyom-26/Wave-Racer`) `riderAnim.ts`의 구조:

```
SIGNALS → STATES → MODIFIERS → IK
```

`prototypes/*/rig.js`는 **렌더러를 모른다.** 2D 렌더러는 관절 각도를 캔버스 실루엣으로, 3D 렌더러는 **같은 숫자**를 Three.js 계층의 `rotation.z`로 소비한다. 어느 쪽도 포즈를 계산하지 않는다.

**따라서 2D↔3D 전환은 되돌릴 수 없는 결정이 아니다.** 애니메이션 자산은 양쪽에 남는다.

전투 큐 5종이 그 층에 대응한다. **`hit`은 STATE가 아니라 MODIFIER다** — 실제 픽스처에서 두 전투원이 같은 tick에 서로 때리므로, 상태로 만들면 맞는 순간 공격이 사라진다. 데이터에서 나온 제약이지 취향이 아니다.

### 1-2. 보드 위 얼굴은 폰에서 읽히지 않는다 — 얼굴 없는 쪽이 낫다

서로를 보지 못하는 두 에이전트가 **독립적으로 같은 결론**에 도달했다. 390px에서 두상 18px, 412px에서 20px. 둘 다 *"내가 출시할 버전은 무얼굴"* 이라고 답했다.

그리고 각자 다른 **부수 효과**를 발견했다:

- **스타일 2**: 얼굴은 **머리 회전 치트의 대가**를 치르고 있었다. 정면 카메라에서 얼굴을 보이려면 두 머리를 ~30° 돌려야 했고, 그래서 인물이 *결투 중에 카메라를 의식하는* 것으로 읽혔다. 얼굴을 없애자 치트가 불필요해지고 둘이 서로를 정면으로 본다. **구도 문제가 얼굴 뒤에 숨어 있었다.**
- **스타일 1**: HUD 컷인을 인장이 아니라 **그림자 흉상**으로 대체. 순수 검정으로도 산발 vs 묶은 머리가 구분되어 접근성 계약(색이 아닌 형태)을 만족한다. — 스타일 2가 자기 보고서에 *"실루엣 흉상이 글리프보다 나을 것 같은데 시도하지 않았다"*고 예측했던 바로 그것이다. **두 안이 교차 검증되었다.**

**잃은 것(공짜가 아니다):** tick별 표정이 화면에서 완전히 사라졌다. 자세가 `hit`(반동 수정자)과 `incapacitated`(무너짐)를 실어 나르지만, 움찔함과 풀린 얼굴은 어디에도 없다. 그림자 흉상은 8틱 내내 동일하다.

**확대 시 약점:** 빈 얼굴이 2D에서 마네킹, 3D에서 달걀로 읽힌다. 폰 크기에선 안 보이지만 클로즈업·마케팅 컷에서는 드러난다.

### 1-3. 미형 얼굴은 코드로 만들기에 최악의 대상이다

두 결과 모두 레퍼런스 수준에 크게 못 미쳤고, **격차는 칠하기가 아니라 비례**였다(두개골 폭, 턱 무게, 눈 높이·간격).

구조적 이유: 사람의 지각은 얼굴 비례에 **몇 퍼센트 오차까지** 반응한다. 절차적 생성의 작업 방식(상수 수정 → 캡처 → 육안 → 재수정)은 그 정밀도에 대해 **대역폭이 압도적으로 낮다.**

INK TIDE가 코드로 성공한 대상(파도·선체·거품·자세)은 전부 사람이 "정답 비례"를 외우고 있지 않은 것들이고, **그 라이더의 얼굴은 헬멧과 바이저 뒤에 있다.** 우연이 아니다.

### 1-4. 고정 카메라는 헥스 인접 6방향 중 4개를 반쪽만 해결한다 (경성 한계)

스타일 2가 측정하고 **Fable이 직접 검산했다.**

flat-top axial → 화면 투영에서, 이웃 6방향은 월드에서 전부 등거리(`√3`)지만 **화면 수평 분리는 등거리가 아니다.** 세 축이 60° 간격이므로, 임의의 고정 yaw φ에 대해 분리량은 `√3·|cos(φ−30°)|`, `√3·|cos(φ−90°)|`, `√3·|cos(φ−150°)|` 세 값을 갖는다.

최소값을 최대화하면 **정확히 `1.732 : 0.866 : 0.866`, 비율 2.0**이고, 최적 yaw는 30°(또는 90°) — **카메라가 이미 앉아 있는 자리다.**

```
최적 yaw: 30 deg
6방향 화면 수평 분리: 0.866, 0.866, 0.866, 0.866, 1.732, 1.732
최대/최소 비 = 2.0000
```

**어떤 고정 yaw도 2:1보다 낫게 만들 수 없다.** 인물 폭이 약 34 월드 단위, `S=30`이면 52단위(여유)와 26단위(**확정 겹침**)로 갈린다.

**스타일 1(2D)도 같은 벽에 부딪힌다** — 같은 `q`에서 `r`이 1 차이면 화면 분리 `0.52·s`인데 인물 키가 `2.5·s`라 가까운 쪽이 먼 쪽을 **65% 가린다.** 그리기 순서는 정상이고 **투영이 문제**다.

**→ 이것은 렌더러 문제가 아니라 보드 설계 문제다.** 해법은 렌더러가 아니라 board space에 있다: 헥스 피치를 넓히거나, 셀 내 점유자에게 lateral offset을 주거나, yaw를 점유 집합에 맞춰 적응시키거나. **Codex가 설계해야 할 1순위 항목.**

### 1-5. 절차적 생성을 어디에 쓸 것인가

| 영역 | 방식 | 근거 |
|---|---|---|
| 보드 캐릭터·자세·모션 | **절차적** | 수량이 무한히 늘어남. 잡졸 20명이 서로 다르게 생기는 게 공짜 |
| 전투 이펙트·천·검로 | **절차적** | 시뮬레이션 상태에서 유도됨 |
| 장면 삽화 | **절차적 또는 생성형** | 44개 이상, 계속 증가 |
| **인물 초상** | **직접 제작 (소수 정예)** | 등장인물 수로 상한. 절차적 생성이 가장 못하는 영역 |

원래의 고통(*"에셋 구하기가 어렵고 비싸다"*)은 **수량에 비례**한다. 초상은 수량이 유한하다.

---

## 2. 확인된 결함 (Codex가 고쳐야 할 것)

### 2-1. 🔴 `signalsFor`의 상대 선택 버그 — 공유 레이어, 실제 버그

`prototypes/*/rig.js`:

```js
const other = frame.pieces.find((p) => p.id !== piece.id) ?? null;
```

상대를 **"나 아닌 첫 번째 말"**로 고른다. 진영도 거리도 보지 않는다. 전투원 3명 이상이면 **아군을 상대로 집어들 수 있고**, `dist`/`closing`이 틀어진 채 SIGNALS 층으로 들어가 포즈와 표정을 구동한다.

한 겹 더: `prevOther`가 **같은 술어를 독립 실행**하므로, 프레임 간 말 순서가 바뀌면 `other`와 `prevOther`가 다른 말이 되어 `closing`이 무의미해진다.

**지금까지의 캡처는 전부 유효하다** — 픽스처 전투원이 2명뿐이라 이 경로가 틀린 적이 없다. **2인 픽스처가 구조적으로 은폐하던 버그**이며, 배치 검증을 돌리지 않았으면 발견되지 않았다.

### 2-2. 🟡 배치별 측정 결과

**스타일 2** (실측, 412×445 실제 stage 크기 기준):

| 배치 | 인물 크기 | 판정 |
|---|---|---|
| 기준(같은 행 인접) | 160px | **통과** |
| 원거리 q0/q7 | 56px | **스케일 실패** |
| 대각 인접 (Δr≠0) | 176px | **겹침 실패** |
| 수직 스택 | 161px | **겹침 실패** |
| 3인 일렬 | 122px | 통과 |
| 포위 1v3 | 140px | 경계 |
| 혼전 6인 | 98px | 경계 |

**스타일 1**: 카메라가 상수(`zoom 1.30, focus (q2.5, r−0.46)`)로 이 픽스처에 맞춰 고정. 자동 카메라로 바꾸면 보드를 벗어나진 않지만 인물이 **절반 크기**가 된다. 원거리 q0/q7에서 49px로 붕괴.

**둘 다 통과한 것:** 어떤 밀도에서도 **오클루전 오류 없음.** 6인 겹침에서도 모든 모서리가 정상 해결되고 아웃라인이 번지지 않으며 셀 틴트·접지 그림자가 올바르게 정렬된다. 색을 빼도 진영이 갈라진다.

### 2-3. 🟡 그 외 (양쪽 공통 또는 개별)

- **이름표 충돌 처리가 없다.** 포위에서 4개, 혼전에서 5개가 겹치고 하나는 우측에서 잘린다.
- **인물이 클론이다.** 같은 진영 3인이 동일 포즈·동일 외형. 리그에 시드가 있지만 체형 변주에 쓰이지 않는다.
- **보드에 per-piece 신원 표시가 없고** HUD가 2카드 고정 — 한 진영 3인 이상은 구분 불가.
- **스타일 2 드로우콜 미병합** — 파츠마다 메시+아웃라인 헐. 6인이면 수백 드로우콜. INK TIDE 예산은 라이더당 4. **병합 + 정점 스키닝은 계상되지 않은 실제 작업이다.**
- **스타일 2 머리 회전은 코드에 명시된 치트**다(무얼굴 변형에서는 제거됨).
- **표정 레이어가 무얼굴 경로에서 죽은 코드**가 된다. 지우거나 초상 전용으로 범위를 명시해야 한다.
- **`evade` / `balance_broken`은 작성됐지만 미검증** — 이 픽스처가 그 큐를 만들지 않는다.
- 스타일 1의 **검로 파편**이 빠른 칼끝 위상에서 재발(probe 3·4·6·7). **클래시 플레어는 실데이터에서 한 번도 발화한 적 없다** — t1–t8에서 칼날 선분이 실제로 교차하지 않는다.
- 배치 probe는 **단일 tick·단일 위상**이다. 시간에 따른 배치 변화는 아무도 테스트하지 않았고, **관전 보드의 진짜 실패 모드는 정지 화면이 아니라 모션일 수 있다.**

---

## 3. Hard invariants (이전 슬라이스에서 상속 — 깨지 말 것)

- **additive-optional 직렬화** — `ScenePage.combat`은 optional이고, `page.combat`이 `undefined`면 기존 52개 인카운터 출력이 **바이트 단위로 동일**해야 한다.
- **renderer boundary** — renderer는 `page.combat`을 **포맷만** 한다. 피해·명중·승패·cue 파생·로그 중요도 필터·피해 합계를 재계산하지 않는다(정본 13). 허용된 연산은 표시 변환뿐(hundredths→정수, tick→ms, 좌표→비율).
- `ScenePage`에 **CSS class / pixel coordinate / DOM selector / Canvas command / image path를 넣지 않는다.**
- **액션 prefix·저장 키 동결**, **route graph 불변**, **신규 의존성 금지**(승인 없이).
- **reduced-motion 최종 정지 상태** — 모든 `animation`/`transition`/`@keyframes`는 `@media (prefers-reduced-motion: no-preference)` 안에만. `reduce`에서 최종 프레임이 올바른 정지 상태여야 한다.
- **`forced-colors: active` 대체** 유지. 캔버스/WebGL 표면은 `sr-only` 시맨틱 대체를 병행해야 한다(현행 `combat-board__table` 선례).
- **진영을 색만으로 구분하지 않는다**(I9). 형태가 1차 단서, 색은 보강.
- **결정론** — `Math.random()` 금지. 시드는 `visual_id`/`(pieceId, tick)` 해시에서.
- 좌표는 **flat-top axial**. 변환은 `1.5q`, `√3(r + q/2)`. 표기는 `(q=…, r=…)`, 절대 `(x, y)` 아님.
- terminal 렌더러(`crates/escape-terminal/src/snapshot.rs`)의 cue 심볼 표와 **짝을 맞춘다. 한쪽만 고치지 말 것.**

---

## 4. 검증 명령

```bash
# 실제 전투 프레임 재생성 (결정론적 — 같은 시드는 같은 바이트)
cargo run -p escape-core --example dump_combat_spectator -- 2

# 프로토타입 캡처 (각 경연 디렉터리에서)
cd prototypes/contest-style1 && npm install
PLAYWRIGHT_CHROMIUM_PATH=/path/to/chrome node capture-playscreen.mjs

# 게임 본체 (WSL)
cargo test -p escape-core
cd web && npm test                    # qa:art-assets + vitest
cd web && npm run qa:storybook:visual  # 5뷰포트, wasm 재빌드 선행 필요
```

**주의(`AGENTS.md` 검증 신뢰 원칙):** subagent의 PASS/FAIL을 그대로 믿지 말고 영향이 큰 검증은 직접 재실행한다. `cargo`/`wasm-pack`/`gh`는 이 머신에서 **WSL에만** 있다.

---

## 5. WP 목록 (순서 고정, WP당 커밋 1개)

> Codex가 이 순서를 바꾸거나 WP를 쪼개도 좋다. 다만 **WP-0과 WP-1을 다른 것보다 먼저** 할 것.

| WP | 내용 | 비고 |
|---|---|---|
| **WP-0** | `AGENTS.md:69` 시각 방향 갱신 | **사용자 확인 필요.** "확정 결정" 절이므로 임의 수정 금지. §0 참조 |
| **WP-1** | `signalsFor` 상대 선택 수정 (§2-1) | 최근접 **적**을 고르고, `prevOther`를 `other`와 **같은 말 id**로 고정. 3인 이상 회귀 테스트 추가 |
| **WP-2** | 보드 공간에서 인접 겹침 해소 (§1-4) | **설계 필요.** 헥스 피치 확대 / 셀 내 lateral offset / 적응형 yaw 중 택. 2:1은 고정 yaw의 경성 한계이므로 카메라 튜닝으로는 못 푼다 |
| **WP-3** | 이름표 de-collision 패스 | 포위·혼전에서 4~5개 겹침 |
| **WP-4** | per-piece 보드 신원 + 가변 HUD | 한 진영 3인 이상 구분 |
| **WP-5** | 표정 레이어 범위 확정 | 무얼굴 보드에서 죽은 코드. 삭제 또는 초상 전용 명시 |
| **WP-6** | 인물 체형 변주에 시드 사용 | 클론 제거 |
| **WP-7** | 모션 중 배치 검증 | 정지 probe만 있음. **관전 보드의 진짜 실패 모드일 수 있음** |

---

## 6. 명시적 범위 밖

- **인물 초상 제작** — 별도 트랙. 직접 제작이 결론이며(§1-3), 절차적 생성으로 다시 시도하지 말 것.
- **스타일 최종 선택** — 아래 §7 참조. 아직 확정되지 않았다.
- **드로우콜 병합 + 정점 스키닝** — 스타일 2를 채택할 때만 발생. 계상되지 않았다.
- 게임 본체 배선. 이 트랙은 `prototypes/`에서만 움직였다.
- `web/src/ui/storybook/ink/` 정리 — 수묵 유산이지만 이번 범위 밖.

---

## 7. 미확정 — Codex가 설계해야 할 것

1. **보드를 2D로 갈 것인가 3D로 갈 것인가.** Fable의 잠정 판단은 **2D(스타일 1)** 였다: 폰 크기 가독성, 런타임 의존성 0, verlet 천, 드로우콜 부채 없음. 3D는 카메라 자유도와 더 나은 얼굴 텍스처를 주지만 **전투원당 ~60 드로우콜**과 병합/스키닝 부채를 진다. **다만 §1-4의 2:1 한계는 두 스타일 모두에 걸린다** — 이건 선택 기준이 아니다.
2. **§1-4를 board space에서 어떻게 풀 것인가.** WP-2. 이게 전투 시스템의 배치 규칙(점유·대형·large-unit footprint)과 맞물리므로 `docs/design/Combat_Hex_Rework_*`와 함께 봐야 한다.
3. **표정을 어디까지 살릴 것인가.** 무얼굴 보드가 확정이면, `incapacitated`에서 그림자 흉상을 교체하는 정도로 잃은 표현을 싸게 회복할 수 있다(스타일 1 제안).
4. **초상 수량과 제작 방식.** 감정이 필요한 순간에만 컷인을 띄우는 구조라면 수량이 더 줄어든다.

---

## 8. 산출물 위치

| 경로 | 내용 |
|---|---|
| `prototypes/README.md` | **연구 기록 전문** — 경과, 캡처가 잡은 결함 전체, 결론 |
| `prototypes/character-rig/` | 기본 프로토타입 (공유 리그 + 두 렌더러 최초 비교) |
| `prototypes/contest-style1/` | 경연 1 — 2D 본 리그 (얼굴 / 무얼굴 / 배치 probe) |
| `prototypes/contest-style2/` | 경연 2 — 3D 셀 셰이딩 (동일) |
| `crates/escape-core/examples/dump_combat_spectator.rs` | 실제 `ScenePage.combat` 덤퍼 |

두 경연작은 **서로를 보지 못하는 별도 worktree**에서 만들어졌고 공유한 것은 `rig.js`뿐이다. 병합하지 않고 형제 디렉터리로 보존했다 — **갈라짐 자체가 기록의 요점**이기 때문이다. 각각 독립 실행 가능하다.

---

## 9. 최종 체크리스트

- [ ] WP-0: 사용자 확인 후 `AGENTS.md:69` 갱신
- [ ] WP-1: `signalsFor` 수정 + 3인 이상 회귀 테스트
- [ ] WP-2: 인접 겹침 board-space 해법 설계 및 적용
- [ ] WP-3~6 순차 진행, WP당 커밋 1개
- [ ] WP-7: 모션 중 배치 검증
- [ ] 스타일 최종 선택 (§7-1) — 근거를 문서에 남길 것
- [ ] 각 WP마다 **캡처로 검증**. 코드 추론으로 시각적 주장을 하지 말 것
- [ ] 실화면 QA 게이트(`npm run qa:storybook:visual`) — 게임 본체에 배선할 때
